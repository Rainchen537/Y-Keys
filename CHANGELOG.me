# Y-Keys AI Changelog

本文件记录 Y-Keys 面向 AI/维护者的工作流水。每次功能、修复、发布、发布事故处理或维护规范调整后追加记录。不要在这里记录 token、证书密码、Apple ID、notary 密码等敏感信息。

用户可见发布历史维护在 `CHANGELOG.md`，GitHub Release 需要同步用户可见发布说明。

## 2026-07-20

- 重做 Y-Keys 应用图标：保留 Y-Project 的浅色圆角底板、深靛蓝和紫粉橙渐变，以三枚修饰键、宽 Command 键和半透明错位键盘层表达“快捷键集合”和“双击左 Command”。
- 通过 `icon/make_icns.sh` 同步生成 `icon/icon_1024.png`、`icon/AppIcon.icns` 和 `docs/icon-256.png`，并完成 1024px、256px 视觉复查。
- 重做菜单栏图标为透明 `18 x 18` template image：两层错位键帽轮廓结合 Command 符号，避免旧实现中不透明白色区域参与模板着色。
- 修复修饰键候选高亮：只按 Command 时 `Command-Z` 与 `Command-Shift-Z` 都可作为前缀候选；继续按 Shift 后，`Command-Z` 因缺少 Shift 立即恢复暗态，只有完整兼容的组合继续高亮。候选名称、副标题和已按键帽现在同步使用强调色，普通主键出现后要求修饰键和主键完整匹配。
- 修复应用快捷键过多时挤压右侧系统分组：每条 lane 与应用/系统容器改用布局计算出的确定宽度，文字降低横向抗压并截断；极端密集数据再按面板宽度收窄 lane。使用 152 条应用快捷键的合成预览确认系统分组仍完整位于右侧且无滚动条。
- 新增 Esc 安全退出：页面存在 Esc 快捷键时，第一次按下显示“再次按下 Esc 退出”，1.2 秒内第二次退出；不存在时单次退出。键盘监听改为可过滤 event tap，浮层处理的 Esc key-down、自动重复与对应 key-up 都返回 `nil`，不会传给原 App。
- 新增 `Tests/ShortcutModelTests.swift`，覆盖 Command / Command-Shift 前缀与完整匹配，以及 Esc 首次提示、限时二次退出和超时重置。
- `build.sh` 在每次构建前从 `IconGen.swift` 同步生成 ICNS 和 README PNG，生成后缺少 ICNS 时直接失败；对临时签名 App 做严格校验，并在 File Provider 立即重建空 FinderInfo 的 Documents 工作区中，对最终内容的无扩展属性临时副本再做严格校验。DMG 框架改为按设备节点重试卸载，解决 Finder 短暂占用返回“资源忙”导致发布中断的问题。
- `release.sh` 先检查公证凭据和清理同版本旧产物，再校验构建后的版本/构建号、严格验证 App/DMG stapled ticket，并通过临时目录原子生成版本化 Release DMG。公证等待连接中断但已取得 submission ID 时，改用内建 `notarytool wait` 继续等待最终状态，避免手工轮询、固定超时误判和重复提交；退出清理改用显式条件，避免 zsh `ERR_EXIT` 在空路径判断处提前终止。
- 准备发布 `v0.1.4`，版本号为 `CFBundleShortVersionString=0.1.4`、`CFBundleVersion=5`；README、公开 changelog 和设计维护说明已同步。
- 基于最终源码运行 `release.sh` 成功完成 App/DMG 双公证与双装订：App 公证 ID 为 `73aa83c3-0cf4-4dbb-8ed4-2db52d484047`，DMG 公证 ID 为 `0ab29774-daa4-41d9-8ff8-3a6da7600a37`；最终 `Y-Keys-v0.1.4.dmg` SHA256 为 `c72777604f54ded21f5e60845edadb8725a916669bed1dc6ffc94758fc4577de`，严格签名、Gatekeeper、stapler 与 `hdiutil verify` 均通过。

## 2026-07-19

- 接入仓库内 vendored 的 `Y-Framework/Permission`，统一首次启动权限汇总和后续 Accessibility、Input Monitoring、键盘监听提示；共享协调器按运行副本与三项状态生成持久化指纹，状态变化后顺序推进，完全相同的警告不再重复弹出。
- 保留 Y-Keys 自己的公开权限查询、System Settings 入口、event tap 重试和重启判断，设置预览环境继续禁止模态权限弹窗。
- 接入仓库内 vendored 的 `Y-Framework/DMG`，`make_dmg.sh` 仅保留产品配置；背景标题、Finder 布局、Applications 链接、签名和最终只读重挂载统一验证，并已用包含空格的测试 App 名完整验证动态名称路径。
- 移除旧的项目专用 `icon/DmgBgGen.swift` 和 `icon/dmg_bg.png`，避免共享框架接入后继续维护重复实现。
- 正式安装版重启改为通过 LaunchServices 打开精确 App 路径，不再使用会强制新实例的 `open -n`。
- 通过共享框架独立编译、Y-Keys 完整构建、DMG 生成与背景/布局截图检查；准备发布 `v0.1.3`，版本号为 `CFBundleShortVersionString=0.1.3`、`CFBundleVersion=4`。
- 完成 `v0.1.3` 发布闭环：源码提交 `88a499377a36432a64983119b7e8226636b0ae41`、tag `v0.1.3`、Release `https://github.com/Rainchen537/Y-Keys/releases/tag/v0.1.3`；最终 `Y-Keys-v0.1.3.dmg` SHA256 为 `0f59a1d82ca387699c96e1668cc868697fe46ce04d8f5ae607fc9ae723c298e3`，GitHub asset 名称、下载链接和 digest 已核对一致，App 公证 ID 为 `f8286ac9-9e12-4a61-a1ed-ba00743194a8`，DMG 公证 ID 为 `f13c4d74-df37-41e4-a79a-fbb5c35288a5`。
- 从上述最终 DMG 覆盖安装到 `/Applications/Y-Keys.app` 并通过 LaunchServices 启动；确认版本 `0.1.3 (4)`、Bundle ID `com.lixingchen.YKeys`、Developer ID 团队 `A94225N8T5`、hardened runtime、严格 codesign、App/DMG stapled ticket 与 Gatekeeper 均通过，且只运行一个 `YKeys` 进程。安装版权限页显示“正式安装版”、辅助功能与输入监控均“已开启”、键盘监听“运行中”；真实双击左 Command 可扫描当前 TextEdit 菜单并显示快捷键浮层，按住 Command 时只有 Command 键帽高亮，浮层在当前显示器可用区域内完整显示且无滚动条。

## 2026-07-18

- 修复从设置页点击“显示快捷键”时扫描到 Y-Keys 自身的问题，改为保留打开设置前的前台 App 并针对它读取菜单快捷键。
- 新增 GitHub Release 手动更新检查，更新页可直接显示最新版、发现新版本或检查失败状态。
- 权限页将辅助功能与输入监控拆分为独立状态和操作，分别使用 `AXIsProcessTrusted` 与 `CGPreflightListenEventAccess` 检测，并提供对应的请求和系统设置入口。
- App 重新激活时会自动复查两项权限；若键盘 event tap 尚未运行，会再次尝试启动监听，覆盖从系统设置授权后返回 App 的恢复路径。
- 权限页新增运行副本诊断，按 `/Applications/Y-Keys.app` 路径、Bundle ID、代码签名有效性和 Developer ID 团队标识区分“正式安装版”与“开发副本”，并可切换到验证通过的安装版。
- 权限记录重置改为后台执行，并将 `Accessibility` 与 `ListenEvent` 两项 TCC 刷新都限定到 `com.lixingchen.YKeys`；`reset_accessibility.sh` 同样会先验证正式签名安装版，再定向重置并重新启动。
- 移除设置窗口常驻期间的 1.5 秒轮询，返回 App 时仍会自动复查。
- 设置预览模式在无辅助功能权限时不再被模态警告阻塞，并在显示窗口前先选择目标页面。
- 共享设置框架增加屏幕可见区域约束和 sidebar identifier 校验；关于页补充产品身份。
- 权限启动警告改为按辅助功能、输入监控和运行副本状态去重：相同状态不会重复弹窗，授权状态变化后仍会提示下一项缺失权限。
- 通过 `NSWorkspace` 激活与终止通知持续记录最近使用的外部 App；设置窗口置前时继续扫描该 App，并拒绝扫描 Y-Keys 自身或已终止的旧目标。
- 权限页新增独立键盘监听运行状态；权限记录部分重置失败时会先刷新设置页各状态，再显示失败原因，同时移除未被引用的 `AppDelegate` 重置实现。
- 更新检查可正确提取带后缀 tag 的数字版本，并在检查失败时向用户显示具体错误信息；`Sources/UpdateChecker.swift` 仍是必须纳入源码提交的构建源文件。
- 将安装路径、Bundle ID 和 Team ID 集中到 `AppBranding`，供设置页和 `AppDelegate` 共用，避免运行副本诊断参数漂移。
- `make_dmg.sh` 的 Finder 布局改为使用显式挂载点的实际名称；布局失败会终止打包，不再静默产出未经布局验证的 DMG。
- 准备发布 `v0.1.2`，版本号为 `CFBundleShortVersionString=0.1.2`、`CFBundleVersion=3`，公开 changelog 日期为 `2026-07-18`。
- 完成 `v0.1.2` 发布闭环：源码提交 `f4e6a1546fac25a6fe8d95f354bb85df5a043f2a`、tag `v0.1.2`、Release `https://github.com/Rainchen537/Y-Keys/releases/tag/v0.1.2`；最终 `Y-Keys-v0.1.2.dmg` SHA256 为 `4403d67753e72b87f935acf9a3f0926a122f05dd46119d4abadb9b95f10b0395`，GitHub asset digest 已核对一致。
- 从上述最终 DMG 覆盖安装到 `/Applications/Y-Keys.app` 并通过 LaunchServices 启动；确认版本 `0.1.2 (3)`、Bundle ID `com.lixingchen.YKeys`、Developer ID 团队 `A94225N8T5`、hardened runtime、严格 codesign、App/DMG stapler 与 Gatekeeper 验证通过。安装版权限页截图确认“正式安装版”、辅助功能与输入监控均“已开启”、键盘监听“运行中”，且只运行一个 `YKeys` 进程。

## 2026-07-13

- 新增项目级 `AGENTS.md`，继承 Y-Project 根规范，并记录 Y-Keys 的仓库、分支、版本字段、DMG 命名和本机安装验证要求。
- 将“每次修改任务必须发布新版本”设为强制完成条件：签名、公证并 staple DMG，发布 GitHub Release，再从最终 DMG 覆盖安装本机 App。
- 修正 `AI_CONTEXT.me` 中 Y-Clip 和 Y-Dock 的旧仓库地址。

## 2026-06-28

- 接入 `Y-Framework/Setting` 共享设置页面框架。
- 在仓库内 vendored `Y-Framework/Setting/YSettingsFramework.swift`，避免单独 clone GitHub 仓库时缺少父级共享框架。
- 新增 `Sources/SettingsWindowController.swift`，提供统一的独立设置窗口，包含通用、权限和关于页面。
- 将设置页 sidebar 统一为 `通用 / 功能 / 权限 / 更新 / 关于`，并补齐功能、更新页面的标准分类内容。
- 调整共享设置框架窗口比例、sidebar 宽度、内容内边距和卡片等宽约束；通过截图修复 Y-Keys 功能页第一张卡片未拉满内容区的问题。
- 将共享设置窗口默认尺寸放大到 `1200 x 810`，并把左侧导航按钮改为居中排列；通过截图复查 Y-Keys 功能页。
- 修正 sidebar 内 App 信息区和导航区的居中方式，改为直接按 sidebar 中心线约束；移除设置页页面标题和 section card 内的静态解释性小灰字。
- 将 sidebar App 信息区顶部间距调为 96，让 logo 保持在左侧上半部分。
- 将菜单栏入口改为简单菜单：打开设置、更多 Y-Project 占位和退出项。
- 更新 `build.sh`，编译时加入仓库内 `Y-Framework/Setting/*.swift`，并保留父级共享框架回退路径。
- 补齐基础用户可见 `CHANGELOG.md` 和 `release.sh`，准备发布 `v0.1.1`。
- 发布 `v0.1.1` 的签名、公证、staple 后 DMG，SHA256：`a7bb0662959bc225c7dffa3419c396f720b5f06a202e7b7b0e1ba559df9363a0`。

## 2026-06-27

- 统一为 Y-Project 标准 AI 维护结构：`AI_CONTEXT.me` 负责工作规范和项目说明，`CHANGELOG.me` 负责维护更新记录。
- 在 `AI_CONTEXT.me` 中引用根目录 `Y_PROJECT_APP_STANDARD.me`。
- 保留原有 Y-Keys 设计约束、核心实现和后续优先级。
- 明确标记当前待补齐能力：独立 GitHub 仓库、公开 `CHANGELOG.md`、签名公证 release 脚本、GitHub MCP / GitHub App release、自动更新和发布后本机验证。
